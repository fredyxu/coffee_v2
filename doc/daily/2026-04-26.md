# 2026-04-26 更新日志

## 完成

- 新增 `store` 持久化模块（初始化/API/actor）
- `wifi_actor` 接入持久化配置读取与保存
- `status` 模型精简并重命名为 `current`
- 分区方案切换为 `single_app_large`，修复固件分区溢出

## 问题与修复

- 新增源文件未被构建系统纳入，导致链接缺符号
- 通过重新触发 CMake 配置并修正构建配置后恢复

## 计划

- 将 WebSocket 配置接入 `store`
- 在 UI 顶部状态栏增加基于 `status_get_current` 的实时刷新
