#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")" && pwd)"
SIM_DIR="${ROOT_DIR}/simulator"
BUILD_DIR="${SIM_DIR}/build"
BIN="${BUILD_DIR}/coffee_sim"

: "${SIM_DISPLAY_INDEX:=1}"

cmake -S "${SIM_DIR}" -B "${BUILD_DIR}"
cmake --build "${BUILD_DIR}" -j 8
SIM_DISPLAY_INDEX="${SIM_DISPLAY_INDEX}" "${BIN}"
