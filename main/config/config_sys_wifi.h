#pragma once

/* ============================================================
 * WiFi 系统配置（STA）
 * ============================================================ */

/* 断线自动重连：1=开启, 0=关闭 */
#define WIFI_AUTO_RECONNECT 1

/* 弱信号阈值（dBm），例如 -75/-80 */
#define WIFI_WEAK_RSSI_THRESHOLD -75

